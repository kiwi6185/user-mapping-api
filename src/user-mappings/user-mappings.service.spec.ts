describe('UserMappingsService', () => {
  it.todo('returns a new userID when the pair does not exist');
  it.todo('returns the existing userID when the pair exists');
  it.todo('returns the existing userID when insert hits a duplicate key');
  it.todo('reads MySQL when Redis is unavailable');
});
