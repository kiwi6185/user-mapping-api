# User Mapping API

NestJS service that resolves a stable `userID` for an `id1` + `id2` pair.

## Dependencies with Docker

```bash
copy .env.example .env
docker compose up -d
npm install
npm run start:dev
```